metadata:
  name: ${project.artifactId}
stringData:
  QUARKUS_DATASOURCE_USERNAME: library-user
  QUARKUS_DATASOURCE_JDBC_URL: jdbc:postgresql://library-db:5432/library-db
  QUARKUS_DATASOURCE_PASSWORD: library-pwd